import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#d35400', // Naranja cálido
    },
    secondary: {
      main: '#c0392b', // Rojo suave
    },
  },
});
export default theme;